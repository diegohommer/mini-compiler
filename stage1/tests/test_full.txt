declare x as int;
declare y as float;

x = 10;
y = 20.5;

if x <= y then
return x;
else
return y;
end;

while x != 0 do
x = x - 1;
end;

            with my_function(x, y) returns int is
begin
return x + y;
end;

// This is a comment

   x = my_function(3, 4); // test same line comment

declare arr as int[10];
arr[0] = 5;
arr[1] = arr[0] * 2;

if arr[1] >= 10 then
return arr[1];
else
return 0;
end;

declare flag as bool;
flag = true;

if flag then
x = x * 2;
